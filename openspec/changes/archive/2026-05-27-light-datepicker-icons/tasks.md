## 1. Style Adjustments

- [x] 1.1 Append the `.input-control::-webkit-calendar-picker-indicator` selector style block to `frontend/src/style.css`, configuring custom color inversion filters (`filter: invert(1)`), brightness, resting opacity (`0.75`), and pointer cursor.
- [x] 1.2 Add the `:hover` pseudo-class for the calendar picker indicator, raising scale transforms to `1.1` and lighting brightness and opacity filters to full (`1`).

## 2. Verification and Compilation

- [x] 2.1 Execute `npm run build` inside the `frontend/` directory to verify clean production compilation of the CSS files.
